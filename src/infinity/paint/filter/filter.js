(function (_) {
    /**
     * Filter keeper object. To register a filter, add a new property
     * to this structure with the following stub:
     * ifFilter.myFilter = function (pixels, width, height, args)
     */
    _.ifFilter = {

    };
})(this);